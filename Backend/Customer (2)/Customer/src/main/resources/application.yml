# service registers under this name
spring:
  application:
    name: customer

#discover ser
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

server:
  port: 2226
  
  

